# todo_shared

Framework-free todo-list state shared by `examples/todo` and
`examples/todo_skia`: `TodoStore`, `TodoEntry`, seed data, and AppBar title
helper. Unit-tested under `dart test`.

**Full docs:** see the docs site (`just docs-serve`) — Examples → todo_shared.
