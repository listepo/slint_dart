name: todo_shared
description: Shared todo-list state for the examples — framework-free store plus seed data and title helper used by examples/todo and examples/todo_skia.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
