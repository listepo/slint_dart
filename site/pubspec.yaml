name: site
description: slint_dart docs site (Jaspr SSG)
version: 0.0.1

environment:
  sdk: ^3.10.0

dependencies:
  jaspr: ^0.23.4
  jaspr_router: ^0.9.0
  markdown: ^7.3.0

dev_dependencies:
  build_runner: ^2.10.0
  build_web_compilers: ^4.8.5
  jaspr_builder: ^0.23.4
  lints: ^5.0.0

jaspr:
  mode: static
