[package]
name = "slint-interpreter-ffi"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
slint-dart-core = { path = "../../slint/rust" }
slint-dart-interpreter = { path = "../interpreter" }
slint = { workspace = true, features = ["compat-1-2", "std", "renderer-software", "software-renderer-systemfonts"] }

[lints]
workspace = true
